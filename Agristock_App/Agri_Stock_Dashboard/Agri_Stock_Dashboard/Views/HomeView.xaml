<UserControl x:Class="Agri_Stock_Dashboard.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
   
        </Grid.RowDefinitions>

        <TextBlock Text="Management General" FontSize="28" FontWeight="Bold" Foreground="#333"/>

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <WrapPanel Grid.Row="0">
                <Border Width="450" Height="200" Background="White" CornerRadius="10" Margin="0,0,20,20" Padding="25">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="15" Opacity="0.1"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="🚜 Utilaje în fermă " Foreground="Gray" FontSize="14"/>
                        <TextBlock x:Name="lblTotalMachinery" Text="0" FontSize="56" FontWeight="Bold" Foreground="#2E7D32" Margin="0,5,0,0"/>
                        <TextBlock x:Name="lblBrokenMachinery" Text="0 cu probleme" Foreground="#D32F2F" FontSize="14"/>
                    </StackPanel>
                </Border>

                <Border Width="450" Height="200" Background="White" CornerRadius="10" Margin="0,0,20,20" Padding="25">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" BlurRadius="15" Opacity="0.1"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="📦 Cantitate Stocată " Foreground="Gray" FontSize="14"/>
                        <TextBlock x:Name="lblTotalStock" Text="0 kg" FontSize="56" FontWeight="Bold" Foreground="#1976D2" Margin="0,5,0,0"/>
                        <TextBlock x:Name="lblWarehouseCount" Text="în 0 depozite" Foreground="Gray" FontSize="14"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

           
            <Image Grid.Row="1" 
                   Source="pack://application:,,,/Assets/FarmingPicture1.jpg" 
                   Stretch="None" 
                   VerticalAlignment="Center" 
                   HorizontalAlignment="Center" 
                   Opacity="0.9"
                   Margin="0,20,0,0">

              
                <Image.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
                </Image.Effect>
            </Image>
        </Grid>
    </Grid>
</UserControl>