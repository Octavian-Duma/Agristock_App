<UserControl x:Class="Agri_Stock_Dashboard.Views.InventoryAlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text=" Stocuri critice (sub 1000 kg)" FontSize="20" FontWeight="Bold" Foreground="#D32F2F" Margin="0,0,0,10" Grid.Row="0"/>

        <DataGrid x:Name="gridLowStock" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,20"  BorderThickness="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produs" Binding="{Binding Product.Name}" Width="200" FontWeight="Bold"/>
                <DataGridTextColumn Header="Depozit" Binding="{Binding Warehouse.Name}" Width="200"/>

                <DataGridTextColumn Header="Cantitate (kg)" Binding="{Binding CurrentQuantityKg}" Width="*" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text=" Stocuri suficiente (peste 10.000 kg)" FontSize="20" FontWeight="Bold" Foreground="#388E3C" Margin="0,10,0,10" Grid.Row="2"/>

        <DataGrid x:Name="gridHighStock" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True"  BorderThickness="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produs" Binding="{Binding Product.Name}" Width="200" FontWeight="Bold"/>
                <DataGridTextColumn Header="Depozit" Binding="{Binding Warehouse.Name}" Width="200"/>

                <DataGridTextColumn Header="Cantitate (kg)" Binding="{Binding CurrentQuantityKg}" Width="*" FontWeight="Bold">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Green"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>