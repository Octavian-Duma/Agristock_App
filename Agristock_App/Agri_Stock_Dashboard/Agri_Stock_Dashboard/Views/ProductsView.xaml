<UserControl x:Class="Agri_Stock_Dashboard.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Analiză centralizată a produselor" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="White" Padding="10" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5">
                <DockPanel>
                    <TextBlock Text="Selectează Produs:" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,10"/>
                    <ListBox x:Name="lstProducts" SelectionChanged="lstProducts_SelectionChanged" BorderThickness="0"/>
                </DockPanel>
            </Border>

            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                <Border Background="#E3F2FD" Padding="25" CornerRadius="10" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Total în Fermă:" Foreground="#1565C0" FontSize="16"/>
                        <TextBlock x:Name="lblTotalProductQty" Text="0 kg" FontSize="48" FontWeight="Bold" Foreground="#0D47A1"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Distribuție pe depozite:" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>

                <DataGrid x:Name="gridProductDistribution" Height="300">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Depozit" Binding="{Binding Warehouse.Name}" Width="*"/>
                        <DataGridTextColumn Header="Locație" Binding="{Binding Warehouse.Location}" Width="150"/>
                        <DataGridTextColumn Header="Cantitate" Binding="{Binding CurrentQuantityKg}" Width="120" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>